# ---------------------------------------------------------------------------
# 运行时数据 / 本地状态（绝不入库）
# 注意：gitignore 不支持行尾注释，# 必须独占一行，否则会被当成 pattern 的一部分
# ---------------------------------------------------------------------------
node_modules/
.arena-*/
.arena-bridge/
.env
.env.*
!.env.example

# 加密后的邮箱密码 + cookie
credentials.json
creds.json
sessions.json

# 含公网隧道地址与 token
mcp-*.json

# 本地绝对路径（含用户名），可用环境变量 ARENA_ARCHIVE_DIR 覆盖
archive-dir.txt

# 本地 issue tracker（docs/agents/issue-tracker.md）。本仓库是公开仓库，
# 议题与 spec 只留在本地，不随仓库分发
.scratch/

*.log
/tmp/

# ---------------------------------------------------------------------------
# 上游资产：README 列出但代码中无任何引用，2.4 MB，仅增大仓库体积
# ---------------------------------------------------------------------------
assets/hero.png

# ---------------------------------------------------------------------------
# 编辑器 / 工具 / 系统
# ---------------------------------------------------------------------------
.vscode/
.idea/
.workbuddy/
.DS_Store
Thumbs.db
*.swp

# ---------------------------------------------------------------------------
# AI 辅助用文档：仅供本地 agent 使用，不随公开仓库分发
# ---------------------------------------------------------------------------
docs/
AGENTS.md
CONTEXT.md
